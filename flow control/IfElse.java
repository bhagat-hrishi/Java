public class IfElse {
    public static void main(String[] args) {
        //Argument of IF statement should be boolean
        //without curlybrace we can take only one statement inside if
        //but it should not by declerative statement
        if(false)
            int a=0;//error

        if(true)
        {
            int a=4;//this is valid
        }
    }
}